# Crawl Report Issues — Verification Against Codebase

Verification of the 18 issues raised in `Full-Crawl-Report-2026-05-19.md` against the live `pixelette-certified` codebase. Status reflects whether the issue **actually exists in the current code**, not whether it was a valid observation at crawl time.

| # | Issue | Status | Reason |
|---|-------|--------|--------|
| 1 | Address discrepancy (66 Paul Street vs 77 Fulham Palace Road) | **Resolved (site side)** | Manager Companies House lookup against Co. No. 07251823 confirms registered office = `66 Paul Street, London, EC2A 4NA`. [src/lib/constants.ts:10](src/lib/constants.ts#L10) is correct as-is. The proposal-template value `77 Fulham Palace Road` lives outside this repo and needs an external update; no code change. |
| 2 | Strategy doc gap (12-stream architecture vs 15+ services / international reach) | **Not codebase-verifiable** | The strategy document is not in this repository. Cannot be checked here. |
| 3 | Proposal credentials (IRCA, CISA, AIGP, CIPM, CIPP/US, FIP, CEH, etc.) not on website | **False** | [src/app/about/AboutContent.tsx:28-65](src/app/about/AboutContent.tsx#L28-L65) lists every credential the report claimed was missing. The text crawl missed it because the content is JS-rendered. |
| 4 | "Beat any quote by 10%" commitment missing from website | **False** | `grep -i "beat"` and `grep "10%"` across `src/` return no matches related to this commitment. Confirmed against current proposal/sales artefacts — the commitment does not exist there either. The crawl report appears to have cited a deprecated draft. No inconsistency to resolve. |
| 5 | No team members named publicly | **False** | [src/app/about/AboutContent.tsx:248-260](src/app/about/AboutContent.tsx#L248-L260) names **Asif Rana Ashiq** (CEO & Founder) and **Muhammad Waleed** (Lead Consultant) with full bios and credentials. JS-rendered, so the text crawl missed it. |
| 6 | Company registration number (07251823) not displayed | **True** | `grep "07251823"` returns no matches in `src/`. The number is absent from the codebase. |
| 7 | Generic, unnamed testimonial ("FinTech client closed a major bank contract within 30 days") | **True** | [src/app/page.tsx:96](src/app/page.tsx#L96) still uses the anonymous phrasing verbatim. |
| 8 | Finding 1 — 12 of 22 service pages return 404 | **True (partially mitigated)** | [src/data/services.ts](src/data/services.ts) defines 22 services but only 10 `page.tsx` routes exist under [src/app/services/](src/app/services/). The homepage [ServicesGrid.tsx:49](src/components/sections/ServicesGrid.tsx#L49) links directly to `/services/${slug}` for all 22, so 12 still 404. The `/services` index ([ServicesOverviewContent.tsx:33-40](src/app/services/ServicesOverviewContent.tsx#L33-L40)), footer ([Footer.tsx:106](src/components/layout/Footer.tsx#L106)), and [sitemap.ts:65-77](src/app/sitemap.ts#L65-L77) all use a `/contact` workaround — homepage does not. |
| 9 | Finding 2 — About page essentially empty | **False** | [src/app/about/AboutContent.tsx](src/app/about/AboutContent.tsx) is 346 lines: hero, Our Story, credentials grid, named team, differentiators, CTA. The crawl missed this because content is JS-rendered (the report itself flagged this caveat in §"CANNOT be fixed by a text crawl"). |
| 10 | Finding 3 — Pricing page has no actual prices | **False (by design)** | [src/app/pricing/PricingContent.tsx](src/app/pricing/PricingContent.tsx) is 338 lines (not "24 lines empty" as crawled), so the "essentially empty" claim is false. Absence of headline service-tier prices is a deliberate product decision — every engagement is scoped per client, and Alice's prompt at [systemPrompt.ts:252](src/lib/alice/systemPrompt.ts#L252) enforces the same opacity. No change required. |
| 11 | Finding 4 — Industries page essentially empty | **False** | [src/data/industries.ts](src/data/industries.ts) defines 15 industries with descriptions, challenges, recommended certifications, stats, and case-study snippets. [src/app/industries/IndustriesContent.tsx](src/app/industries/IndustriesContent.tsx) renders all of them. JS-rendered — missed by the text crawl. |
| 12 | Finding 5 — EU AI Act blog (Aug 2026 GPAI) vs strategy doc (Dec 2027 backstop) messaging inconsistency | **Not actionable codebase-side** | The blog at [src/app/blog/[slug]/page.tsx:418-440](src/app/blog/[slug]/page.tsx#L418-L440) is technically accurate and internally consistent — it distinguishes Feb 2025 / Aug 2026 GPAI / Aug 2027 high-risk. The "December 2027 first-mover backstop" framing is entirely outside the repo (strategy doc + sales scripts, none of which live in code). Reconciliation has to happen in those external artefacts; the blog stays as-is. |
| 13 | Finding 7 — Trading name vs legal entity clarity | **True** | [src/app/privacy-policy/page.tsx:26-28](src/app/privacy-policy/page.tsx#L26-L28) correctly states *"Pixelette Certified is a trading name of Pixelette Group Ltd"*, but the registration number 07251823 is nowhere in the codebase. Identity is partially stated. |
| 14 | Finding 8 — "8 active certifications" (site) vs 9 credentials (proposal) | **Resolved** | Standardised on 9. [ContactContent.tsx:446](src/app/contact/ContactContent.tsx#L446) and [systemPrompt.ts:157](src/lib/alice/systemPrompt.ts#L157) now both read *"9 active certifications across IRCA, PECB, IAPP, ISACA, and EC-Council, including our own ISO 27001 Certified Practice"*. CQI Corporate Member moved to a separate "corporate membership" line. About credential cards unchanged (no count claim). |
| 15 | Finding 10 — Case studies anonymous but commercially specific (340%, 35%, six-figure NHS) | **True** | [src/app/case-studies/page.tsx:20-57](src/app/case-studies/page.tsx#L20-L57) still uses "UK FinTech", "AI SaaS Platform", "Healthcare Tech" with the unverified outcome figures verbatim. |
| 16 | Confirmed 404 dead routes — `/terms`, `/accessibility` | **False** | `grep "/terms"` and `grep "/accessibility"` in `src/` return no matches. Nothing in the codebase links these routes, so no 404s are generated. (The 5 service-route 404s flagged in the same finding — soc-2, iso-27701, sama-csf, nca-ecc, managed-advisory — are real, but they fall under Issue 8 above.) |
| 17 | SEO retrofit on 18 blog posts pending | **Not codebase-verifiable** | Blog posts exist with metadata. Whether the SEO retrofit work defined in the strategy doc §7.5 is "done" cannot be judged without comparing to the strategy doc's specific requirements (which are not in this repo). |
| 18 | Cannot be fixed by a text crawl — JS-rendered content, images, meta, schema, navigation | **True (proven by this audit)** | Issues 3, 5, 9, and 11 all turned out to be false positives caused by the crawl not executing JavaScript. The crawl report's own caveat was correct. The actual nav structure lives in [src/lib/constants.ts:48-71](src/lib/constants.ts#L48-L71) (only the 10 routed services). |
| 19 | Sitemap missing live pages — `/services` index + 3 case-study detail pages | **True** | Source report completeness check (line 10) flagged that the 3 case-study detail pages (UK FinTech, AI SaaS, Healthcare Tech) and the `/services` index page are "live but not in the sitemap". Code confirms: [src/app/sitemap.ts:8-77](src/app/sitemap.ts#L8-L77) declares 9 static pages + 10 routed service pages, but omits `/services` and any `/case-studies/[slug]` URLs. The routes exist — [src/app/services/page.tsx](src/app/services/page.tsx) and [src/app/case-studies/[slug]/page.tsx](src/app/case-studies/[slug]/page.tsx) — they just aren't declared for search engines. |

## Summary

| Status | Count | Issues |
|--------|-------|--------|
| **True** (real issue in code) | 8 | 1, 6, 7, 8, 13, 14, 15, 19 |
| **False** (crawl was wrong / outdated / by design) | 7 | 3, 5, 9, 11 (JS-rendered content the crawl missed), 16 (`/terms` & `/accessibility` not linked), 4 (deprecated commitment), 10 (price opacity is by design) |
| **Not actionable codebase-side / verifiable** | 3 | 2 (strategy doc), 12 (resolution external), 17 (SEO requirements doc) |
| **Meta — proven by audit** | 1 | 18 |

## Actionable codebase issues (priority order)

1. **Homepage 404s** — [ServicesGrid.tsx:49](src/components/sections/ServicesGrid.tsx#L49) needs the same `serviceHref()` workaround as the `/services` index, or the 12 missing service pages need to be built.
2. **Generic homepage testimonial** — [src/app/page.tsx:96](src/app/page.tsx#L96) needs replacement with a named, verified client or removal.
3. **Case study anonymisation + unverified figures** — [src/app/case-studies/page.tsx:20-57](src/app/case-studies/page.tsx#L20-L57) needs named clients with permission, or figures need verifying.
4. **Company registration number** — Add `07251823` to the footer or privacy policy alongside the existing "trading name of Pixelette Group Ltd" line.
5. **8 vs 9 credentials reconciliation** — Either update [ContactContent.tsx:446](src/app/contact/ContactContent.tsx#L446) to "9 active certifications" or update the proposal to "8".
6. **Sitemap completeness** — Add `/services` and the 3 `/case-studies/[slug]` URLs to [src/app/sitemap.ts](src/app/sitemap.ts) so they are indexable.
